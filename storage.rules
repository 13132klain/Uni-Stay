rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read access to all files (for development and CORS)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true; // Temporarily allow all writes for testing
    }
  }
}
